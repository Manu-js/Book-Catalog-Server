## Install MySqlWorbench
- Execute BookDB.sql in MySqlWorbench

## Start project
  
  - Clone the repository 
  ` git clone URL` and go to project folder with `cd ProjectFolder`
  - Install all the dependencies
  `$ npm install` / `$ yarn install`
  - Run the server 
  `$ nodemon index.js`
